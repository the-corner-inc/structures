# .gitignore

**Path:** `.gitignore`\
**Role:** Recommended: Git exclusions

## Purpose

Excludes installed dependencies, local secrets, caches, and generated build output.

## Guidelines

Ignore `.env` and local variants while keeping `.env.example`. Include output directories used by the selected hosting adapter, plus test reports and coverage artifacts.
