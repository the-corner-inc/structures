# post-table.tsx

**Path:** `src/features/posts/post-table.tsx`\
**Role:** Optional: TanStack Table

## Purpose

A headless table integration when post lists need sorting, filtering, column visibility, or selection.

## Guidelines

Keep columns typed to the public post model and supply semantic table markup. Choose either client or server pagination deliberately; synchronize server pagination with query keys and route search parameters.
