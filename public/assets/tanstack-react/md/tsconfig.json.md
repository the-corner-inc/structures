# tsconfig.json

**Path:** `tsconfig.json`\
**Role:** Required: TypeScript configuration

## Purpose

Enables strict TypeScript checking and React JSX with bundler module resolution.

## Guidelines

Keep aliases consistent with Vite and the test runner. Include generated route types and Vite declarations, and exclude output directories. Type checking does not validate runtime input.
