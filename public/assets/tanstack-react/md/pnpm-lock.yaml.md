# pnpm-lock.yaml

**Path:** `pnpm-lock.yaml`\
**Role:** Generated: pnpm dependency lock

## Purpose

Records exact dependency resolution for the pnpm example toolchain.

## Guidelines

Commit changes produced by the package manager and install with a frozen lockfile in CI. If using npm or another manager, use its lockfile instead of keeping multiple competing locks.
