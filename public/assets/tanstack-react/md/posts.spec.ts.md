# posts.spec.ts

**Path:** `tests/e2e/posts.spec.ts`\
**Role:** Example: browser test

## Purpose

Checks browsing, opening a detail page, and editing a post through the UI.

## Guidelines

Include a hard refresh on a dynamic route and navigation through browser history. Assert resulting content and persistence rather than timing assumptions.
