# error-boundary.tsx

**Path:** `src/components/feedback/error-boundary.tsx`\
**Role:** Recommended: error UI

## Purpose

A reusable route error component with recovery controls.

## Guidelines

Use the router error-component props and offer an appropriate retry or navigation action. Report diagnostic details through the server or client logger without echoing secrets into the page.
