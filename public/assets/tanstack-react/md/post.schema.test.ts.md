# post.schema.test.ts

**Path:** `src/features/posts/post.schema.test.ts`\
**Role:** Example: unit test

## Purpose

Tests post validation at the input boundary.

## Guidelines

Cover representative valid input, missing required values, length limits, and invalid identifiers. Include actual product rules rather than tests that merely repeat schema declarations.
