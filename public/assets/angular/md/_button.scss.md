# _button.scss

Shared button styling for cases that are not owned by a dedicated Angular component.

```scss
.button {
  display: inline-flex;
  align-items: center;
  justify-content: center;
}
```

Prefer a reusable button component when behavior, accessibility, variants, or loading states are involved.
