# site.webmanifest

**Path:** `public/site.webmanifest`\
**Role:** Optional: installable application metadata

## Purpose

Application name, icons, theme colors, and display preferences for browser installation.

## Guidelines

Link it from the root document and provide the icons it references. A manifest alone does not provide offline behavior; introduce a service worker only when needed.
