# Project

A project lives in one repository. It can be a simple application or a monorepo containing multiple
applications and packages.

Use the folder structure suited to the project's stack. When a product spans separate projects, group
them under a `Product` folder.
