/**
 * Drizzle Database Schema
 * 
 * This is a PLACEHOLDER schema file.
 * Define your database tables here using Drizzle ORM syntax.
 * 
 * Better-auth will auto-generate auth tables (users, sessions, etc.)
 * Add your custom tables here as needed.
 * 
 * Reference: https://orm.drizzle.team/docs/sql-schema-declaration
 */

// Example custom table (commented out):
// import { pgTable, serial, text, timestamp } from "drizzle-orm/pg-core"
//
// export const structures = pgTable("structures", {
//     id: serial("id").primaryKey(),
//     name: text("name").notNull(),
//     data: text("data").notNull(),
//     createdAt: timestamp("created_at").defaultNow()
// })
