# utils.ts

**Path:** `src/lib/utils.ts`\
**Role:** Optional: small shared utilities

## Purpose

Small, genuinely shared helpers such as class-name composition.

## Guidelines

Prefer a named domain module as responsibilities grow. Do not add one-off wrappers around standard JavaScript APIs or re-export server modules through this file.
