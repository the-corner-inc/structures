# seed.ts

**Path:** `scripts/seed.ts`\
**Role:** Optional: development seed data

## Purpose

Creates deterministic sample data for local development and test environments.

## Guidelines

Make repeated runs predictable, guard against production targets, and avoid embedding credentials. Keep schema migrations separate from seed data.
