# post-detail.tsx

**Path:** `src/features/posts/post-detail.tsx`\
**Role:** Example: feature UI

## Purpose

Renders a single post using the detail query or loader result.

## Guidelines

Accept a post ID or typed data according to the chosen loading pattern. Render user content safely and keep editing controls conditional on permissions without relying on UI checks for authorization.
