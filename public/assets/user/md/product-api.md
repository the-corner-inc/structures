# product-api

The product's backend API project. Keep its server code, business logic, data access, and API tests
here, using a folder structure suited to its backend stack.

Coordinate behavior with the other projects through `shared-spec`.
