# tokens.css

**Path:** `src/styles/tokens.css`\
**Role:** Optional: design tokens

## Purpose

CSS custom properties for shared colors, spacing, radii, and themes.

## Guidelines

Use semantic token names and check contrast in each theme. Keep tokens independent of individual routes so feature styles can share them.
