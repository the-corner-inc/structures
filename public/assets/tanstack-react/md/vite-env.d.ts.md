# vite-env.d.ts

**Path:** `src/types/vite-env.d.ts`\
**Role:** Recommended: Vite types

## Purpose

Adds Vite ambient typing for asset imports and `import.meta.env`.

## Guidelines

Reference `vite/client`. Additional environment declarations describe types only; they do not replace runtime validation in the environment modules.
