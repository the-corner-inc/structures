# login.tsx

**Path:** `src/routes/_public/login.tsx`\
**Role:** Optional: authentication

## Purpose

Public sign-in route at `/login`.

## Guidelines

Render the feature sign-in form and validate any return URL as an internal application destination before redirecting. Display authentication failures without revealing whether a private account exists.
