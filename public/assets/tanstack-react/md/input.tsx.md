# input.tsx

**Path:** `src/components/ui/input.tsx`\
**Role:** Example: UI primitive

## Purpose

A styled input that retains the native input contract.

## Guidelines

Pass through `id`, `name`, `type`, `value`, event handlers, and accessibility attributes. Connect visible labels and error descriptions in the form using the control.
