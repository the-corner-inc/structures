# routing.md

**Path:** `docs/routing.md`\
**Role:** Recommended: routing guide

## Purpose

Records file naming, layouts, dynamic parameters, search validation, and error conventions.

## Guidelines

Show concrete route filenames and resulting URLs. Include loader responsibilities, page metadata, and how to regenerate the route tree. Explain `_public` versus `_auth`, optional `{-$lang}` matching and validation, and root `robots[.]txt.ts` / `sitemap[.]xml.ts` handlers. Document the equivalence of sibling layout files and directory `route.tsx` files.
