# _typography.scss

Global font faces, type scales, line heights, and reusable typography mixins.

```scss
$font-family-body: Inter, system-ui, sans-serif;
$line-height-body: 1.5;
```

Centralizing these values keeps headings and body copy consistent without coupling components to fixed sizes.
