# settings.json

**Path:** `.vscode/settings.json`\
**Role:** Optional: shared editor configuration

## Purpose

Enables the chosen formatter and consistent editor behavior for this project.

## Guidelines

Exclude generated route code from format-on-save if the generator owns its formatting. Do not hide source or test errors globally.
