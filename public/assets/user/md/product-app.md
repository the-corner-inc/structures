# product-app

The product's application project. Keep its user interface, API integration, and application tests
here, using a folder structure suited to its frontend stack.

Follow the product requirements in `shared-spec` and reuse common assets from `shared-assets`.
