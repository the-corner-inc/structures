# button.tsx

**Path:** `src/components/ui/button.tsx`\
**Role:** Example: UI primitive

## Purpose

A shared button with consistent styles and typed native button props.

## Guidelines

Preserve focus indicators, disabled behavior, and accessible names. Default non-submit actions to `type="button"`; let actual form submit buttons opt into submission.
