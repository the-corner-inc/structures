# example

A small, complete feature used to demonstrate how page-specific code is grouped.

- `example.page.ts` owns the view and user interaction.
- `example.service.ts` contains the feature's data access or business logic.

Replace this folder with a domain name such as `orders`, `profile`, or `dashboard` in a real app.
