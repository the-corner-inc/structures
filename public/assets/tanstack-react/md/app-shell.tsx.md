# app-shell.tsx

**Path:** `src/components/layout/app-shell.tsx`\
**Role:** Example: layout component

## Purpose

Arranges the header, navigation, and page content.

## Guidelines

Accept children or render the route outlet at a clearly defined level. Include a skip link and responsive navigation, and avoid duplicating the root document elements.
